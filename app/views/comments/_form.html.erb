<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :'coach name' %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :'comment' %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit "Submit To This Goal" %>
  </p>
<% end %>