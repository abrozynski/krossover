<h1>User Leader Board</h1>
 
 <hr >
 <p>In Krossover Kredits you compete with other coaches to earn the most Kredits. See the top 10 coaches below. </p>

<hr >

<table class="table table-striped">
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>School</th>
    <th>Sport</th>
    <th>Total Kredits</th>
    <th colspan="4"></th>
  </tr>
 
  <% @user.each do |user| %>
    <tr>
      <td><%= user.first_name %></td>
      <td><%= user.last_name %></td>
      <td><%= user.school_name %></td>
      <td><%= user.sport %></td>
      <td><%= user.cumulative_kredit %></td>
      <td><%= link_to 'Show', user_path(user) %></td>
    </tr>
    <% end %>
</table>

